<div {{attributes}} >


<h1>Quiz</h1>
    {{ form_start(form) }}
    {{ form_widget(form.questions.vars.button_add, {label: '+ Add Item', attr: {class: 'btn btn-outline-primary'} } ) }}
    <button type="submit" class="btn btn-success" formnovalidate="">Send</button>
    {{ form_end(form) }}

</div>
