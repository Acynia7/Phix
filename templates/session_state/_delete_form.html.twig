<form method="post" action="{{ path('app_session_state_delete', {'id': session_state.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ session_state.id) }}">
    <button class="btn">Delete</button>
</form>
