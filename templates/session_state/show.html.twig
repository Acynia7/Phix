{% extends 'base.html.twig' %}

{% block title %}SessionState{% endblock %}

{% block body %}
    <h1>SessionState</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ session_state.id }}</td>
            </tr>
            <tr>
                <th>State</th>
                <td>{{ session_state.state }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_session_state_index') }}">back to list</a>

    <a href="{{ path('app_session_state_edit', {'id': session_state.id}) }}">edit</a>

    {{ include('session_state/_delete_form.html.twig') }}
{% endblock %}
